## Décision de composant — [rôle en une ligne]

**Besoin (rôle)** : qui, fait quoi, dans quel contexte, avec quelle importance
**Décision** : Réutiliser `X` / Composer `A + B + C` / Variant `X.variant=y` / Créer `Nom`
**Palier atteint** : 1 / 2 / 3 / 4

**Pour** :
- …
**Contre** :
- …

**Alternatives écartées et pourquoi** :
- Palier 1 — `X` : ne convient pas car …
- Palier 2 — composition … : ne convient pas car …
- Palier 3 — variant … : ne convient pas car …

**Tokens requis** : aucun / existants (`--…`) / nouveaux → demande dans SIGNALEMENTS.md
**Usages prévus** : [écrans, ≥ 2 si création]
**Action** : `npx shadcn@latest add <nom>` (sur `main`) / recette à documenter / `ds-variant` / `ds-build` (mode custom) / `ds-figma-component` (nœud …)
**Référence Figma** (mode intent, si un nœud existe) : URL · relevé le [date] · état : conforme / divergent
